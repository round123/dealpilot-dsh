## No Negative Echo

生成最终产物及其包装时，包括标题、文件名、正文、注释、标签、commit、
PR 和交付说明，只描述最终采用的状态，假设读者没看过本次会话。

- 会话里的否决、中间尝试和措辞纠正，只当作控制信息，不要让它们成为最终产物的命名或叙述中心。
- 对每个交付面分别判断：不知道本次会话的读者需要这条信息吗？省略会不会导致不准确、不安全、误导或兼容性信息缺失？它是不是任务开始时已提交或用户确认状态中的真实变化，而且当前交付面需要解释它？
- 「不要提 X」不是让你写「无 X」。标题、文件名、开篇和标签应从正向目标重新生成，不要逐词修改被否文案。
- 保留真实的基线变化、已经执行的外部操作，以及必要的技术名称、诊断、测试和快照。任务开始前已有的用户改动不算被否内容。
- 不要把与本任务无关的改动写进本次 commit、PR 或交付说明。对比、引用、审计和迁移说明，只在用户要求或当前交付面确实需要时保留。
- 写完后通读全部用户可见内容及其包装，包括文件名、元数据和 hook 改写。内容发生变化后重新检查，不要另加「已清理」或「无残留」类声明。

## Project Philosophy

DealPilot 的基本假设是：软件面对的是开放世界，而不是一组可以预先枚举完毕的输入。产品不是把人的工作翻译成程序步骤，而是为能够理解世界、参与判断并持续修正的 Agent 提供行动空间。

### 智能与控制

`LLM` 是解释和决策层，`Harness` 是边界和执行层。二者不是同一件事的两种实现，也不应互相替代：

- 智能来自对语境的解释，而不是来自规则数量。
- 控制来自对能力和副作用的约束，而不是来自规定思考顺序。
- 模型可以选择下一步；系统必须保证每一步都可观察、可验证、可追溯。
- 业务意义在运行时形成，系统约束只定义什么可以发生以及如何安全地发生。

### 开放世界

- 不以预先想象的分类穷举现实。
- 不以内部模型的方便，要求外部世界使用同一套语言。
- 信息可以是结构化的、叙述性的、模糊的、矛盾的或尚未命名的；系统都应能承载它。
- 结构是对信息的暂时解释，不是信息本身。解释可以被重写，证据不应被抹去。
- 任何未被当前理解的内容，仍然是未来理解的输入，而不是无效数据。

### 记忆与证据

系统的记忆应当是可重新解释的材料，而不是一次性转换的结果：

- 保留观察到的内容、它的来源以及产生它的上下文。
- 允许多个解释并存，直到新的证据或人的判断使其收敛。
- 让 Agent 能够回到证据，修正过去的判断，而不必依赖当时的隐式状态。
- 派生结果应能追溯到输入；删除信息必须是显式、可审计的行为。

### 能力而非流程

工具是 Agent 可以调用的能力，不是工作流中的固定节点。能力应当语义稳定、组合自由，并让 Agent 看见足够的状态来决定下一步。工具可以保证权限、格式、边界、幂等和副作用，但不应替 Agent 规定业务思考路径。

优秀的 Harness 不是隐藏复杂性，而是把复杂性变成 Agent 可以观察和处理的状态：失败有原因，部分完成有记录，重试有依据，冲突可呈现，行动可回溯。

### 不确定性与协作

不确定性是工作的正常状态，不是异常分支。Agent 应能表达暂定理解、证据强度、冲突和待确认事项；用户应能看到关键判断并改变它。确认的对象是可见的行动及其依据，而不是对不可见推理的笼统授权。

### 工程判断

- 优先扩大 Agent 能看到、理解和修正的范围，再扩大自动执行的范围。
- 只把安全、权限、完整性、可恢复性和审计交给确定性机制；把开放式语义留给 Agent。
- 追求最小且稳定的协议，让能力可以替换、组合和演进。
- 设计应支持反复观察、局部行动、反馈和再次规划，而不是只优化一次性成功。
- 评估真实表现时，关注系统在模糊、意外、变化和不完整输入下的恢复能力，而不只是理想路径的通过率。

### 判断问题

每项设计都应回到几个问题：

1. 它是在增加 Agent 的理解空间，还是在增加预设流程？
2. 它是在保护边界，还是在替业务做决定？
3. 信息经过它之后，是否仍可读取、追溯和重新解释？
4. 当假设错误或环境变化时，Agent 是否有机会发现并修正？

所有实现、评审、测试和文档都以 `LLM + Harness` 的这组原则为最高判断依据。

## 本地调试契约

DealPilot 是独立的 DSH 插件，`dsh-super-injector` 只作为本机开发环境的运行时手术台。Injector 可以
注入、热重载、侧挂、验证和回滚插件；它的源码、依赖、配置和开发工具不进入 DealPilot 仓库或正式包。

### Injector 约定

- 开发态插件由 injector 管理；正式发布仍由 DSH 的标准 bundle 装配。
- 注入对象是构建后的标准插件包。修改代码后重新构建，injector 监听的运行目录会自动 reload；需要时可手动调用
  `dev_reload_package`。
- 任何 reload 都应验证 host 与 client 是否同时更新。失败时保留旧版本，依据返回结果和日志决定重试或回滚。
- 注入、侧挂和卸载只改变开发运行态，不绕过 DealPilot 的权限、确认、幂等和审计边界。

### 调试原则

调试遵循一个可重复的闭环：观察当前状态，做最小修改，重新构建并让 injector 应用变更，再验证行为和界面。
优先使用临时 Workspace 或只读检查；保留失败证据、运行日志和版本关系，使 Agent 能继续定位和修正。

### 记录

调试记录应说明现象、证据、判断、处理、验证和遗留风险，区分插件代码、injector 装配、DSH host、client 与业务数据。
